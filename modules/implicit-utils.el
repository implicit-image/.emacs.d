

;; (defmacro +utils/toggle-mode (mode state)
;;   "Toggle MODE to STATE (`'on' or `'off')"
;;   (let ())
;;   `(lambda ()
;;      (mode ,val)))




(use-package restart-emacs)


(provide 'implicit-utils)
